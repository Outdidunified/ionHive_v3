import 'package:flutter/material.dart';

class Chargingconnector extends StatelessWidget {
  const Chargingconnector({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CHARGING'),
      ),
    );
  }
}
